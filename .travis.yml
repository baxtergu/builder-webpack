language: node_js

sudo: false

cache:
  apt: true
  directory:
    - node_modules

node_js: lts/*

install:
  - npm install -D
  - cd ./test/smoke/template
  - npm install -D

scripts:
  - npm run coverage
